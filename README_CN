2014/06/19
  *) 数据文件保存完整的会话名(session_name).
  *) 从数据库查询完整的会话名并进行解析参数.

2014/06/17
  *) 修改del为rm.
  *) 数据库增加alias字段.
  *) 增加使用别名来建立会话连接.

使用例子:
 rm 可删除 session_name 和 alias
 rm用法
  rm root@192.168.1.1   # 删除会话名数据
  rm test               # 删除别名数据
  rm neiwang55          # 删除别名数据
 rn 将会话(session_name)建立一个别名(alias)
  rn root@192.168.1.1 wangguan    # 把会话名root@192.168.1.1 别名为 wangguan
  rn root@192.168.1.55 neiwang55  # 把会话名root@192.168.1.55 别名为 neiwang55 
 dbclient 可直接使用别名建立连接
  dbclient neiwang55     # 将与 192.168.1.55 建立连接
  dbclient wangguan      # 将于 192.168.1.1 建立连接

2014/06/11
  在原来的版本中dbclient加入sqlcipher数据库保存用户密码信息, 存放到~/.dropbear/user.db.
  dbclient增加了2个参数：
    ls： 用来显示记录的服务器列表。
    del： 用来删除记录的服务器信息。
  dbclient当用户登陆成功后，会将密码记录到加密的数据文件中，方便下次自动登陆。不需要频繁的输入帐号密码。
  以后使用scp和dbclient都不用输入密码了哦。:-)
